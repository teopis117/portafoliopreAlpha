import * as React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
    return (
        <div>
            <Typography variant="h4" component="div">Bienvenido a mi sitio web</Typography>
            <Typography variant="body1" component="div">Aquí es donde podrías incluir una breve introducción sobre ti.</Typography>
        </div>
    );
}

export default Home;
